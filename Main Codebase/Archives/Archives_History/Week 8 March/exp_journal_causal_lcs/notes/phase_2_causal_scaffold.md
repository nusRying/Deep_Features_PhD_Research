# Phase 2 Causal Scaffold Notes

Added isolated Causal-LCS modules for this experiment only.

Current scope:

- configuration dataclass
- dataset loader for the current repo layout
- metadata builder and JSON artifact writer
- causal fitness policy scaffold
- causal subsumption policy scaffold
- local `main.py` entrypoint

Current limitation:

- no real propensity estimation yet
- no feature transformation pipeline yet beyond raw numeric column selection
- no benchmark runner yet

These are the next implementation targets inside this experiment branch.